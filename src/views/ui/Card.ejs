<section class="card glass-panel <%= typeof className !== 'undefined' ? className : '' %>">
  <% if (typeof title === "string" && title) { %>
    <h2><%= title %></h2>
  <% } %>
  <%- typeof body !== "undefined" ? body : "" %>
</section>
